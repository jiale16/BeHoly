<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 步骤2：Device Owner 配置（大部分用户无法设置，收进三个点菜单） -->
    <item
        android:id="@+id/action_device_owner"
        android:title="@string/btn_configure_device_owner" />

    <!-- 日志显隐：整体显示/关闭实时日志区 -->
    <item
        android:id="@+id/action_show_log"
        android:title="@string/menu_show_log" />
    <item
        android:id="@+id/action_hide_log"
        android:title="@string/menu_hide_log" />

    <!-- 关于：应用介绍（原主界面顶部一句介绍移至此处） -->
    <item
        android:id="@+id/action_about"
        android:title="@string/menu_about" />
</menu>
